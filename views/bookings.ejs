<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Bookings</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <% layout('layout') %>
<h2 class="text-2xl font-bold mb-6">My Bookings</h2>

<ul class="space-y-4">
  <% if (bookings.length === 0) { %>
    <p class="text-gray-500">No bookings yet.</p>
  <% } else { %>
    <% bookings.forEach(booking => { %>
      <li class="bg-white shadow-md rounded-lg p-4">
        <h3 class="font-semibold text-lg"><%= booking.event.title %></h3>
        <p class="text-gray-600">Seats: <%= booking.seats %></p>
        <p class="text-sm text-gray-500">Booked on: <%= booking.createdAt.toDateString() %></p>
      </li>
    <% }) %>
  <% } %>
</ul>

</body>
</html>